# 🚀 Загрузка проекта на GitHub

## Шаги для загрузки на GitHub

### 1. Создайте репозиторий на GitHub

1. Перейдите на https://github.com/vmfoberon-star
2. Нажмите кнопку **"New"** или **"+"** → **"New repository"**
3. Название репозитория: `chef-professional-site`
4. Описание: `Professional chef website for Gilad Ventura - Catering services`
5. Выберите **Public** или **Private**
6. **НЕ** добавляйте README, .gitignore или лицензию (они уже есть)
7. Нажмите **"Create repository"**

### 2. Загрузите код

После создания репозитория выполните команды:

```bash
cd "F:\Users\vmfob\Desktop\chef-professional-site"

# Remote уже настроен, просто загрузите:
git push -u origin main
```

Если репозиторий уже создан, просто выполните:

```bash
git push -u origin main
```

### 3. Проверка

После загрузки проект будет доступен по адресу:
**https://github.com/vmfoberon-star/chef-professional-site**

## 📝 Текущий статус

- ✅ Git репозиторий инициализирован
- ✅ Все файлы закоммичены
- ✅ Remote origin настроен: `https://github.com/vmfoberon-star/chef-professional-site.git`
- ✅ Ветка переименована в `main`
- ⏳ Ожидается создание репозитория на GitHub и команда `git push`

## 🔧 Если возникнут проблемы

### Если репозиторий уже существует:
```bash
git push -u origin main
```

### Если нужно изменить URL:
```bash
git remote set-url origin https://github.com/vmfoberon-star/chef-professional-site.git
```

### Если нужно создать репозиторий с другим именем:
```bash
git remote set-url origin https://github.com/vmfoberon-star/НОВОЕ-ИМЯ.git
```

---

**После создания репозитория на GitHub выполните: `git push -u origin main`**

